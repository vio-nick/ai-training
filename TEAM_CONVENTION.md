# 骨质疏松骨折风险 AI 模型：双人协作约定

## 1. 适用范围与共同目标

本约定适用于本仓库内由两名成员共同完成的模型开发与构建工作。临床资料整理、系统综述、文献筛选和前端工作由项目整体团队另行协同；模型组以经确认的特征字典、数据使用许可和研究方案为输入。

共同目标是构建一个**以骨质疏松性骨折为主要预测终点**的、可复现且可解释的多模态风险预测原型，并给出可用于基层筛查的轻量化方案。模型工作必须服务于临床问题，不以单纯提高单个指标为目标。

以下原则不可变更：

- 主任务的标签、预测时点和纳入人群须在训练前写入数据字典；跌倒风险仅可作为系统综述或补充分析中的代理结局，不能在未说明的情况下替代骨折终点。
- 仅使用具有明确来源、使用许可和访问权限的数据；任何个体级医疗数据进入仓库前均须去标识化，并满足伦理审批及数据管理要求。
- 不得把原始数据、可识别信息、访问令牌或私有模型权重提交到 Git。
- 每次可合并的改动都必须可运行、可追溯，并能由另一位成员复现关键结果。

## 2. 分工与低依赖接口

两人以“责任模块”划分工作。目标是**先并行开发，最后一次集成**，而不是让成员 B 等待成员 A 的完整数据管线。成员姓名可在首次项目会议后填入下表。

| 角色 | 可独立完成的工作 | 独立交付物 | 只在集成时交换的内容 |
| --- | --- | --- | --- |
| 成员 A：数据与基线负责人 | 数据接入、质量检查、清洗、特征工程、切分策略、传统基线模型 | 数据字典、数据质量报告、数据处理管线、逻辑回归/随机森林/XGBoost 基线结果 | 按共享契约导出的特征表、切分清单和基线指标 |
| 成员 B：融合与评估负责人 | 使用合成样例或公开样例搭建训练/评估框架，多模态融合、模型比较、校准、解释性、轻量化推理 | 可插拔的训练评估框架、早期/晚期/混合融合实现、评估报告模板、SHAP 分析、推理接口和模型卡模板 | 将成员 A 的最终特征表接入适配器，回传集成后的结果和数据问题清单 |

### 共享契约（一次冻结，变更需版本化）

两人只需在正式开发前共同确认以下最小契约，之后各自按契约并行开发：

1. 标签字段、预测时间窗、样本唯一标识和允许的特征字段；
2. 特征表的列名、数据类型、单位、缺失值编码和模态标记；
3. 训练/验证/测试切分清单的格式，以及评估脚本的输入输出格式；
4. 统一的随机种子、主要指标和结果文件命名规则。

契约写入 `docs/data_dictionary.md` 或 `docs/interfaces/` 后视为 `v1`。兼容性变更直接更新文档；不兼容变更必须新建 `v2` 并保留适配器，不能让另一人的分支停工等待重构。

两人共同确认研究问题、主要终点、数据泄漏规则、模型入选标准和对外结论；这些是少量同步点，不构成日常开发的前置依赖。跨模块需求创建 Issue，写明输入、输出、影响范围和验收条件；各自模块的核心逻辑由负责人维护。

## 3. 并行研发流程与阶段门

阶段门只约束集成，不阻塞两人的独立开发。若数据可得性、标签质量或伦理状态不满足条件，应记录阻塞原因，不得用未经批准的数据绕过阶段门。

| 阶段 | 成员 A（数据线） | 成员 B（模型线） | 是否需要等待 |
| --- | --- | --- | --- |
| 0. 契约冻结 | 确认字段、标签、切分和质量规则 | 确认训练/评估接口、指标和输出格式 | 仅此阶段需要一次共同确认 |
| 1. 并行开发 | 用获批/公开数据完成清洗、质量报告和基线 | 用合成样例或公开样例完成模型、融合、评估和推理框架 | 不需要等待 |
| 2. 各自验收 | 固定数据版本、切分清单和数据产物 | 固定配置、测试、指标脚本和模型卡模板 | 不需要等待 |
| 3. 一次集成 | 提供契约 `v1` 的正式特征表 | 通过适配器接入正式特征表并运行全套评估 | 只在集成窗口协作 |
| 4. 共同复核 | 解释数据质量和偏倚 | 解释性能、校准和轻量化取舍 | 集成后共同完成 |

成员 B 在成员 A 的真实数据尚未就绪时，必须使用固定的合成 fixture 或公开样例继续开发；成员 A 也可独立运行基线和数据检查，不等待融合代码完成。

## 4. 数据、实验与结果管理

### 数据规则

- 原始数据放在仓库外的受控位置；仓库只保存脚本、公开数据下载说明、字段映射、合成样例和汇总统计。
- 每个数据版本必须记录来源、许可/伦理状态、获取日期、样本量、纳入排除流程、字段含义、单位、缺失率和已知偏倚。
- 训练、调参和测试集必须按个体（必要时按时间或中心）隔离。任何使用测试集调参、标准化拟合或特征筛选的行为都视为数据泄漏。
- 需统一维护随机种子、切分索引与预处理器；结果表中必须标注使用的数据版本和配置版本。

### 实验规则

- 每次正式实验使用配置文件或等价的命令行记录，至少包含数据版本、特征集合、切分、随机种子、模型超参数和运行环境。
- 模型比较以相同切分、相同预处理边界和预先约定的指标为前提；不能只汇报最佳一次运行结果。
- 推荐报告 AUROC、AUPRC、灵敏度、特异度、F1、校准曲线与 Brier score；类别不平衡时同时报告阳性比例和阈值选择方法。
- 解释性分析必须在独立验证/测试数据上完成，并写明解释方法及其局限；SHAP 图不能单独作为因果证据。
- 每个候选模型都应有模型卡，至少说明用途、输入、训练数据范围、性能、亚组/偏倚风险、局限和禁止用途。

## 5. Git、代码与评审

### 分支和提交

- `main` 仅保留可复现的稳定版本，禁止直接推送；日常集成可使用 `dev`（如仓库启用）。
- 分支命名：`feat/<topic>`、`fix/<topic>`、`data/<topic>`、`experiment/<topic>`、`docs/<topic>`。
- 提交格式：`type(scope): 简短说明`，例如 `feat(data): add gait feature quality checks`、`experiment(fusion): compare late fusion baselines`。
- 一个提交只处理一个逻辑目标。不得混入格式化、无关重构、数据文件或临时输出。

### Pull Request（PR）

每个 PR 应关联 Issue，并写清：目标、改动、数据/配置影响、复现命令、指标变化、风险和回滚方式。独立模块的 PR 可在另一人尚未完成其模块时先行开发、测试和审阅；只有跨模块集成 PR 需要两人共同确认。

合并前的最低检查项：

- [ ] 代码可在干净环境中运行，依赖已声明；
- [ ] 相关单元测试、数据校验或最小烟雾测试通过；
- [ ] 不包含原始数据、隐私信息、密钥、大型临时文件或未经许可的材料；
- [ ] 实验结果可定位到数据版本、配置和随机种子；
- [ ] 若影响结果，README、数据字典、实验记录或模型卡已同步更新；
- [ ] 独立模块已完成至少一次异步审阅；跨模块集成已由两人共同审阅设计、泄漏风险和临床表述。

## 6. 沟通与决策

- 以异步更新为主，每周一次短同步只讨论共享契约、真实阻塞点和集成计划；不要求等待对方汇报后才能继续编码。
- 关键决定记录在 Issue、PR 或 `docs/` 中，尤其是标签定义、特征增删、数据切分、阈值和模型选择；口头结论不作为唯一依据。
- 发生指标冲突时，优先排查标签质量、泄漏、切分差异、样本偏倚和校准，再讨论更复杂的模型。
- 无法达成一致时，先保留双方可复现实验和证据，由项目负责人/指导教师按临床合理性、数据质量、可复现性和基层可及性裁决。

## 7. 完成定义

一项模型功能或实验仅在同时满足以下条件时标记为完成：实现已合并；测试或校验通过；结果能复现；数据与配置可追溯；局限已写明；另一成员能够解释其输入、输出、关键假设及失败场景。

本约定与项目申请书不一致时，以伦理审批、数据使用协议和指导教师确认的研究方案为准，并应在仓库中留下更新记录。
