# 成员 B 执行日志

## 概率输出与分级补充

- 推理结果现同时返回 0%--100% 概率、`low`/`medium`/`high` 和低风险/中风险/高风险展示值。
- 固定边界为：`p < 0.3` 低风险，`0.3 <= p <= 0.7` 中风险（含边界），`p > 0.7` 高风险。
- 展示分级与模型封版的 `decision_threshold` 独立；分级只描述当前回顾性自报跌倒者模型分数。

## 2026-09-08

- 新增 `gstride_fall_v1` 版本化输入契约，校验六特征、ID、标签、数值类型、有限值和版本兼容性。
- 实现配置驱动的单模态训练、验证集平衡准确率阈值、测试评估和轻量推理。
- 实现 Brier/ECE 校准、阈值影响、错误案例、亚组模板、逻辑回归逐样本贡献和随机森林全局重要性。
- 实现早期、晚期、混合融合及 `error`、`available_only`、`impute_zero` 缺失模态策略。
- 运行正式 GSTRIDE 工作流；测试指标与已有基线一致。
- 运行无标签融合 fixture smoke test；未报告真实融合性能。
- 完整单元测试：19 项通过。

当前交付完成成员 B 的训练、评估、推理和融合框架职责。正式结论仍限于 GSTRIDE 六特征的回顾性跌倒者识别；BMD、医院纵向跌倒或骨折数据到位前，真实多模态融合和临床终点验证仍未开始。
