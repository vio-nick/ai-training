# 成员 B 集成审计：GSTRIDE v1

审计日期：2026-09-08

| 审计项 | 状态 | 证据 |
| --- | --- | --- |
| 正式数据接口 | 通过 | `contracts.py` 校验版本、六特征、标签、ID、类型和有限值 |
| 训练/阈值边界 | 通过 | 预处理只在训练分区拟合，阈值只由验证集选择 |
| 测试边界 | 通过 | 测试集仅用于最终指标、校准、错误案例和解释 |
| 推理边界 | 通过 | 公共推理不读取标签，也不重新拟合预处理器 |
| 正式单模态结果 | 通过 | 成员 B 工作流指标与成员 A 基线一致 |
| 融合框架 | fixture 通过 | 早期、晚期、混合及缺失策略有测试和 smoke test |
| BMD/医院融合性能 | 未运行 | 尚无合规、版本兼容的第二模态数据 |
| 亚组结果 | 不可用 | 六特征契约没有人口学或临床字段 |

复现检查：`D:\anaconda3\python.exe -m unittest discover -s tests` 共 19 项通过；两个成员 B 运行脚本均成功。

接入医院数据前必须完成伦理审批、数据授权、去标识化、新数据/接口版本、纵向终点定义、时间外验证、外部验证、校准和预定义亚组评估。
